# Docker Compose Environment Variables
# ===================================
# Development-only credentials (not for production)
# These are used by docker-compose.yml

DATABASE_URL=postgresql://postgres:postgres_dev_pass@postgres:5432/sum_api
REDIS_URL=redis://redis:6379
NODE_ENV=development
PORT=3000
